#include <iostream>
#include <climits>
using namespace std;

int main() {
	//정수형
	//int n_int = INT_MAX;
	//short n_short = SHRT_MAX;
	//long n_long = LONG_MAX;
	//long long n_long = LLONG_MAX;

	unsigned int a; //unsigned : 음의 값 저장 X

	//실수형
	float d = 0.1;

	char b;//작은 문자형(보통 한글자), 큰따옴표 사용 불가
	//"" >> 명시적으로 null 문자가 포함되어 있음.(String)

	//bool형 : 0 혹은 1
	bool c;
	return 0;
}